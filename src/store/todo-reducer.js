import { todoList } from "./mock-items";

export const todoReducer = (state = todoList, action) => {
  switch (action.type) {
    // case value:

    //     break;

    default:
      return state;
  }
};
